name: Tests

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest]
        python-version: ["3.12"]

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install system dependencies (Ubuntu)
        if: matrix.os == 'ubuntu-latest'
        run: |
          sudo apt-get update
          sudo apt-get install -y inkscape pandoc python3-dev build-essential

      - name: Install system dependencies (macOS)
        if: matrix.os == 'macos-latest'
        run: |
          brew install inkscape pandoc

      - name: Install uv
        run: |
          curl -LsSf https://astral.sh/uv/install.sh | sh
          echo "$HOME/.cargo/bin" >> $GITHUB_PATH

      - name: Install dependencies
        run: |
          $HOME/.cargo/bin/uv pip install --system pip setuptools wheel
          $HOME/.cargo/bin/uv pip install -e ".[dev]"

      - name: Run tests
        run: |
          $HOME/.cargo/bin/uv run pytest -v

      - name: Run type checking
        run: |
          $HOME/.cargo/bin/uv run mypy src tests

      - name: Run linting
        run: |
          $HOME/.cargo/bin/uv run black --check src tests
          $HOME/.cargo/bin/uv run isort --check-only src tests
          $HOME/.cargo/bin/uv run ruff check src tests
